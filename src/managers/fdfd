    // addProductToCart = async(product) => {  
    //     let productToCart= {id:this.cart.length+1, product: [product]}
        
    //     this.cart.push(productToCart)
    //     await fs.promises.writeFile(this.path, JSON.stringify(this.cart, "null", 2), "utf-8")

    //     if (this.path.length >= 1) {
    //          let productList = await this.readCart()
    //          productToCart = [...productList,{id:productList.length+1 , product: [product]}]
    //     }

    
    // } 